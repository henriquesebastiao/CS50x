## As etapas que o compilador executa para cheagr a um binário:

- Preprocessing (pré-processamento)
    - Processa as bibliotecas incluídas no código:
        ```
        #include <stdio.h>
        #include <stdlib.h>
        ```
- Compiling (compilação)
- Assembling (montagem)
- Linking (ligação)